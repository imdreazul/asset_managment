<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ==================== LIST VIEW ==================== -->
    <record id="view_asset_accessory_list" model="ir.ui.view">
        <field name="name">asset.accessory.list</field>
        <field name="model">asset.accessory</field>
        <field name="arch" type="xml">
            <list string="Asset Accessories" editable="bottom">
                <field name="name" string="Accessory Name"/>
                <field name="active" widget="boolean_toggle"/>
            </list>
        </field>
    </record>

    <!-- ==================== FORM VIEW ==================== -->
    <record id="view_asset_accessory_form" model="ir.ui.view">
        <field name="name">asset.accessory.form</field>
        <field name="model">asset.accessory</field>
        <field name="arch" type="xml">
            <form string="Asset Accessory">
                <sheet>
                    <widget name="web_ribbon" title="Archived" bg_color="text-bg-danger"
                            invisible="active"/>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1>
                            <field name="name" placeholder="Accessory Name..."/>
                        </h1>
                    </div>
                    <group>
                        <field name="active" widget="boolean_toggle"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- ==================== SEARCH VIEW ==================== -->
    <record id="view_asset_accessory_search" model="ir.ui.view">
        <field name="name">asset.accessory.search</field>
        <field name="model">asset.accessory</field>
        <field name="arch" type="xml">
            <search string="Search Asset Accessories">
                <field name="name" string="Accessory Name"
                       filter_domain="[('name', 'ilike', self)]"/>
                <separator/>
                <filter string="Active" name="active" domain="[('active', '=', True)]"/>
                <filter string="Archived" name="archived" domain="[('active', '=', False)]"/>
                <group expand="0" string="Group By">
                    <filter string="Active" name="group_active" context="{'group_by': 'active'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- ==================== ACTION ==================== -->
    <record id="action_asset_accessory" model="ir.actions.act_window">
        <field name="name">Asset Accessories</field>
        <field name="res_model">asset.accessory</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="view_asset_accessory_search"/>
        <field name="context">{'search_default_active': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first Asset Accessory
            </p>
            <p>Accessories define supplementary items that come with or belong to assets.</p>
        </field>
    </record>

</odoo>

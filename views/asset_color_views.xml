<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ==================== LIST VIEW ==================== -->
    <record id="view_asset_color_list" model="ir.ui.view">
        <field name="name">asset.color.list</field>
        <field name="model">asset.color</field>
        <field name="arch" type="xml">
            <list string="Asset Colors" editable="bottom">
                <field name="name" string="Color Name"/>
                <field name="active" widget="boolean_toggle"/>
            </list>
        </field>
    </record>

    <!-- ==================== FORM VIEW ==================== -->
    <record id="view_asset_color_form" model="ir.ui.view">
        <field name="name">asset.color.form</field>
        <field name="model">asset.color</field>
        <field name="arch" type="xml">
            <form string="Asset Color">
                <sheet>
                    <widget name="web_ribbon" title="Archived" bg_color="text-bg-danger"
                            invisible="active"/>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1>
                            <field name="name" placeholder="Color Name..."/>
                        </h1>
                    </div>
                    <group>
                        <field name="active" widget="boolean_toggle"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- ==================== SEARCH VIEW ==================== -->
    <record id="view_asset_color_search" model="ir.ui.view">
        <field name="name">asset.color.search</field>
        <field name="model">asset.color</field>
        <field name="arch" type="xml">
            <search string="Search Asset Colors">
                <field name="name" string="Color Name"
                       filter_domain="[('name', 'ilike', self)]"/>
                <separator/>
                <filter string="Active" name="active" domain="[('active', '=', True)]"/>
                <filter string="Archived" name="archived" domain="[('active', '=', False)]"/>
                <group expand="0" string="Group By">
                    <filter string="Active" name="group_active" context="{'group_by': 'active'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- ==================== ACTION ==================== -->
    <record id="action_asset_color" model="ir.actions.act_window">
        <field name="name">Asset Colors</field>
        <field name="res_model">asset.color</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="view_asset_color_search"/>
        <field name="context">{'search_default_active': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first Asset Color
            </p>
            <p>Colors help identify and filter assets by their physical color.</p>
        </field>
    </record>

</odoo>
